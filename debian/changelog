pgpid (0.1.0-1) unstable; urgency=medium

  [ Mnêmê ]
  * pgpid: the C tool, in C over gpgme, arrives from the foodjis repository
    where it was written. Twenty-four actions, replacing bl-pgpid and
    bl-pgpkey action for action.
  * Nine message catalogues; French complete for the messages themselves.
  * Build-depends on gettext, help2man and pandoc, which the packaging needed
    already and did not say. Not on libgpgme-dev nor pkgconf: the keyring is
    read by asking gpg now, and the binary links nothing but libc.
  * Depends on ${shlibs:Depends}: there is a compiled binary now.
  * The tools the printing and scanning actions call are Recommends rather
    than Depends — pgpid reads certificates without any of them.
  * Reads the keyring by asking gpg rather than through gpgme, which could
    not see user attributes and answered questions we had not asked.
  * trustdb: one action where ownertrust and update_trustdb were two; export
    produces what import consumes, and import weighs every line against what
    this machine already says rather than overwriting it.
  * certify, cert_revoke, cert_get --recurse, cert_sigs, cert_email
    --set-primary, cert_property expire, secret_del, token_del, token_cache:
    the actions the shell had, and several it did not.
  * system_adduser / system_deluser / system_confhome / system_users /
    system_admins: a certificate opens and closes a local account.
  * gen_u4 takes a civil status typed or read off a passport, in one action.
  * The sixteen helps, in nine languages, saying what the shell's said.
  * The program completes itself: bash-completion asks the binary.
  * cert_get asks the keyserver the way HKP is asked — one op=get request,
    whatever the search is — instead of only reaching out for an address or a
    fingerprint. An entity identifier is looked up by its body, which is what
    the old spelling (u4=…) and the new one (u4…) share, so a certificate
    minted before the separator went away is found again.
  * secret_scan reads a camera again, as bl-pgpkey scan did: --camera, and
    pgpid-qrscan falls back to it when no sheet is named. Which camera is
    asked of the kernel rather than left to zbarcam's default, and named when
    there is more than one; the size is the largest the camera streams up to
    1920x1080, since 640x480 cannot resolve a secret printed as a single QR
    code. --cameras answers the list on its own, for a caller with nobody to
    ask.
  * gen_u4 says when the reference transliteration did not take a name as it
    was typed — MÜLLER as MULLER — which the shell said and this did not, and
    --verify shows the whole civil status for correction before computing.
  * secret_totoken sets the codes it announces. It called token_code without
    --replace, which only verifies, so a freshly written card kept its factory
    PIN and Admin code while a random pair was printed. token_code now refuses
    a new code given without --replace.
  * secret_totoken asks before wiping a card that carries something, instead
    of refusing outright, and asks nothing of a blank one.
  * secret_scan says when it has every fragment, and what it put back
    together, and shreds the working directory it made itself — without
    --workdir it left the pieces of a reconstituted secret key under /tmp.
  * pgpid-gen mints an identifier without a passport again, and --verify shows
    the civil status for correction before anything is computed, as the shell
    did. The country keeps the shell's FRA default.
  * Nothing suggests `bl-security` any more: the package does not pull the
    shell libraries in, so a person told to run it would find nothing.
  * A star for each character typed at a code or passphrase prompt, as
    `bl-interactive input --password` showed; backspace takes one back and
    Ctrl-U the lot. A terminal put into no-echo is given back even when the
    question is interrupted.
  * Three programs fewer to spawn, and so three packages fewer to want:
    basenc and split gave way to the base64url codec this binary already
    carried — the secret no longer passes through a file of its own on the way
    out — and curl to 'gpg --fetch-keys', which fetches and imports in one go.
  * One renderer for both sheets. The fragment sheet went through pandoc and
    pdfcrop, the sticker sheet through pdfunite and pdfjam: a TeX distribution
    and poppler, some 275 MB, to put six lines and a picture on a page and to
    repeat one drawing across another. rsvg-convert was already here for the
    business card and it writes PDF, so both pages are SVG now and one
    program draws them. The sticker page defines the card once and places it
    with <use>, the card carrying its own QR as a data: URI.
  * The dependencies say what an action actually spawns. scdaemon was missing
    and is half the tool: gnupg does not pull it. pandoc and
    texlive-extra-utils are gone — 275 MB that nothing calls at run time any
    more; pandoc stays a Build-Depends, which is where it always belonged,
    since it turns the manual pages from markdown into roff. imagemagick is
    gone too: a scanned PDF is read by the graphicsmagick already here, which
    given one output name writes the first page and stops, where `convert`
    wrote scan-0-0.png and scan-0-1.png and neither was the name we opened.
    The bash libraries move up to Depends: pgpid-gen and pgpid-qrscan source
    four of them at their first line and do not start without, and both are
    in this package. sudo stays a Recommends, being the only thing whose
    absence merely narrows what an action can do. curl is gone from both,
    nothing calls it.
  * PGP, not OpenPGP: what pgpid reads and writes are PGP certificates, being
    compatible with OpenPGP and LibrePGP both. The card's own specification
    keeps its name.
  * French is complete again — 595 of 595. The catalogues had drifted away from
    the sources, so gettext found nothing and every translated page came out in
    English. The other eight languages fall back to English where they drifted.
  * The manual pages list the actions again — the generator stopped at the
    first blank line and the actions had been grouped behind one. The
    completion harvests what each option takes, so a path completes to files,
    a printer to what CUPS knows, and a camera to /dev/video*.
  * Carries the business-card template cert_tobizcard renders, in
    /usr/share/pgpid/svg/card.svg. bash-libs keeps its copy, which bl-pgpid
    still reads; the two paths are disjoint.
  * pgpid-gen and pgpid-qrscan call the actions by the names they have had
    since they were put into groups. Both died on their first real step.

 -- Mnêmê <mneme@foopgp.org>  Mon, 14 Sep 2026 15:33:32 +0200

pgpid (0.0.7-1) unstable; urgency=medium

  [ jjbrucker ]
  * move all nok sample to scan_nok/ (using mrz.traineddata data model)
  * update bash-libs submodule + new testpassport.sh + move printer question
  * allow pgpid-gen to generate u5 OpenPGP IDentifier

 -- jjbrucker <jjbrucker@foopgp.org>  Sun, 10 May 2026 18:11:35 +0200

pgpid (0.0.6-1) unstable; urgency=medium

  [ JJ ]
  * bin/pgpid-qrscan Now support qrversion=4, qrversion=5
  * pgpid-qrscan: big refactoring and improvements
  * update: manpages, descriptions, bash-libs submodule
  * code cleaning + fix pgpid-gen's completion
  * move old stuff into new dir 'oldies/' (will save ~10 Mo at install)
  * completly rewrite pgpid-gen to greatly improve its UX
  * prepare packaging (gbp dch ; fix debian/control + debian/patches)

 -- piseb <piseb@mailo.com>  Mon, 22 Dec 2025 11:06:12 +0100

pgpid (0.0.5-1) unstable; urgency=medium

  [ JJ ]
  * update dependencies

 -- piseb <piseb@mailo.com>  Mon, 08 Dec 2025 16:25:46 +0100

pgpid (0.0.4-2) unstable; urgency=medium

  [ JJ ]
  * fix bin install

 -- piseb <piseb@mailo.com>  Thu, 06 Nov 2025 23:12:59 +0100

pgpid (0.0.4-1) unstable; urgency=medium

  * refactor: restore ln s files and remove ugly clean file

 -- piseb <piseb@mailo.com>  Mon, 27 Oct 2025 13:31:57 +0100

pgpid (0.0.3-1) unstable; urgency=medium

  [ Sébastien Picardeau ]
  * release v0.0.3

  [ Jbar ]
  * clean code, using new bash-lib: bl-pgpid

 -- piseb <piseb@mailo.com>  Wed, 22 Oct 2025 16:45:59 +0200

pgpid (0.0.1-2) UNRELEASED; urgency=low

  * add ffmpeg dependency

 -- Sébastien Picardeau <piseb@mailo.com>  Thu, 28 May 2025 10:00:00 +0200

pgpid (0.0.1-1) UNRELEASED; urgency=low

  * Initial release.

 -- Sébastien Picardeau <piseb@mailo.com>  Thu, 15 May 2025 10:09:29 +0200
