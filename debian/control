Source: pgpid
Section: utils
Priority: optional
Maintainer: Sébastien Picardeau <piseb@mailo.com>
Build-Depends: debhelper-compat (= 13),
  gettext,
  libgpgme-dev,
  pandoc,
  pkgconf
Standards-Version: 4.5.1
Homepage: https://foopgp.org
Rules-Requires-Root: no
Vcs-Git: https://codeberg.org/foopgp/pgpid.git
Vcs-Browser: https://codeberg.org/foopgp/pgpid

Package: pgpid
Architecture: any
Depends: gnupg,
  bashlibs-common,
  bashlibs-convert,
  ${shlibs:Depends},
  ${misc:Depends}
Recommends: sudo,
  qrencode,
  zbar-tools,
  libgfshare-bin,
  poppler-utils,
  texlive-extra-utils,
  librsvg2-bin,
  cups-client,
  pandoc,
  graphicsmagick
Suggests: facedetect,
  tesseract-ocr,
  gpg-wks-client,
  imagemagick,
  curl
Description: OpenPGP certificates that say whose they are
 The PGP ID model gives an OpenPGP certificate a decentralised entity
 identifier: who this key belongs to, said once and checked by people rather
 than by an authority.
 .
 pgpid reads and changes certificates through that model — identifiers,
 validity, ownertrust, addresses, vCard properties, avatars — vouches for
 other people, drives an OpenPGP security key, and puts a secret key on paper
 as QR codes so that any three of five fragments rebuild it.
 .
 The system actions open and close local accounts and hand out administrator
 rights, so they want sudo: without it they still run, but only for root, and
 there is then no SUDO_USER whose keyring and trust they can read — use their
 --from option to name one.
 .
 Two older shell programs ship alongside: pgpid-gen, which generates a
 certificate from a passport, and pgpid-qrscan, which moves secrets onto a
 smartcard. They need the packages under Suggests.
